generated: '2026-08-08'
method: probed
source: https://app.cafex.com/audit-search-rest/search
format: non-rfc9457
note: >-
  CafeX publishes no OpenAPI document and no error reference page, so this catalog cannot be
  derived from a spec. What is recorded here is the error envelope observed from a live anonymous
  probe of the documented endpoint, plus the authorization-server error observed from the token
  endpoint. Only statuses actually observed or explicitly documented are listed — no status codes
  are inferred.
envelopes:
- name: platform-error
  applies_to: https://app.cafex.com/audit-search-rest
  content_type: application/json
  fields:
  - name: timestamp
    type: string
    description: ISO 8601 timestamp of the failure.
  - name: status
    type: integer
    description: HTTP status code, repeated in the body.
  - name: error
    type: string
    description: HTTP reason phrase.
  - name: path
    type: string
    description: Request path that produced the error.
  problem_details: false
  note: Spring-style error body; no `type`/`title`/`detail`/`instance` RFC 9457 members.
- name: oauth-error
  applies_to: https://auth.cafex.com/authserver
  content_type: application/json;charset=UTF-8
  fields:
  - name: error
    type: string
    description: OAuth 2.0 error code (RFC 6749 section 5.2).
  - name: error_description
    type: string
  - name: incident_id
    type: string
    description: CafeX-specific correlation identifier for support.
  problem_details: false
problems:
- status: 400
  envelope: platform-error
  error: Bad Request
  observed_on: POST https://app.cafex.com/audit-search-rest/search
  observed_body: '{"timestamp":"2026-08-08T15:21:21.917Z","status":400,"error":"Bad Request","path":"/audit-search-rest/search"}'
  cause: >-
    Request body missing or failing validation — page, timestampFrom and timestampTo are required,
    and pageSize must not exceed 100.
  remediation: Send a JSON body containing page.pageSize, timestampFrom and timestampTo.
  method: probed
- status: 400
  envelope: oauth-error
  error: invalid_client
  observed_on: POST https://auth.cafex.com/authserver/token
  observed_body: '{"error":"invalid_client","error_description":"Client ID or the Client secret are invalid","incident_id":"<uuid>"}'
  cause: client_id / client_secret missing or not valid for the tenant.
  remediation: >-
    Create a client application under Tenant settings > Management > Client applications and use
    the issued Client ID and Client secret.
  method: probed
undocumented:
  note: >-
    CafeX documents no error reference, no error-code registry and no per-status response
    catalog. 401/403/404/429/5xx behaviour for the Audit Events API is unpublished; it is not
    recorded here rather than being guessed.
x-evidence:
- url: https://app.cafex.com/audit-search-rest/search
  method: POST
  http_status: 400
  content_type: application/json
  fetched: '2026-08-08'
- url: https://auth.cafex.com/authserver/token
  method: POST
  http_status: 400
  content_type: application/json;charset=UTF-8
  fetched: '2026-08-08'
